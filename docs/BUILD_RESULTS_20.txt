PAYAW Milestone 20 production-build status

TypeScript application checking passed with `npm run check`.

A fresh Vite bundle was not produced in this packaging environment. Installing the locked Vite dependency failed because the configured package gateway returned HTTP 503 Service Temporarily Unavailable. The stale Milestone 19 `dist` output was removed and is not included in the Milestone 20 release.

Build in an environment with normal npm registry access:

  npm install
  npm run build
